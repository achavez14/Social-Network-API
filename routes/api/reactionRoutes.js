const express = require('express');
const router = express.Router();

// Define CRUD operations for reactions
// Get all reactions
router.get('/', (req, res) => {
});

// Add a new reaction
router.post('/', (req, res) => {
});

// Remove a reaction by ID
router.delete('/:id', (req, res) => {
});

module.exports = router;